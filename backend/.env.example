DATABASE_URL=postgres://gym:gym@localhost:5433/gym_db
JWT_SECRET=change_me
PORT=4000
# Opcional: orígenes permitidos para CORS separados por coma. Por defecto ya
# incluye dev (localhost:5173), docker-compose (:8080) y el frontend de Render.
# CORS_ORIGINS=https://mi-dominio.com,http://localhost:5173
